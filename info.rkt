#lang setup/infotab
(define name "pdf-render")
(define blurb
  '("A hacky interface to libpoppler that renders PDF files to dc% and picts. Very minimal for now..."))
(define primary-file "main.rkt")
(define categories '(media io))
(define repositories '("4.x"))
(define scribblings '(("main.scrbl" ())))
(define release-notes '((p "The initial (hacky!) version.")))
